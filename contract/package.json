{
  "name": "contract.most.box",
  "version": "2.0.0",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network BaseTestnetSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network BaseMainnet",
    "clear": "rm -rf ./typechain-types && rm -rf ./artifacts && rm -rf ./cache",
    "npm": "npm i"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^17.2.3",
    "hardhat": "2.26.3"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-ethers": "3.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "ethers": "^6.15.0"
  }
}

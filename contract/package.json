{
  "name": "contract.most.box",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network BaseTestnetSepolia",
    "clear": "rm -rf ./typechain-types && rm -rf ./artifacts && rm -rf ./cache",
    "npm": "npm i"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "dotenv": "^17.1.0",
    "hardhat": "^2.25.0"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.9",
    "@openzeppelin/contracts": "^5.3.0",
    "ethers": "^6.15.0"
  }
}